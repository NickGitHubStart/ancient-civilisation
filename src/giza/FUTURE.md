# Giza — Zukunft

This Three.js scene is a **measured teaching model**, not the final fidelity.

**Later:** rebuild as a real CAD model, or in Unity (or similar), so it plays like a video game and is more precise than the current procedural mesh.

Do not treat the current interiors (passages as boxes, corbels as strips, no per-block core) as the end state. The JSON in `data/` (Petrie / Cole originals) should survive that rebuild — it is the source of truth for dimensions, not the Three.js geometry.

The same note is shown as a dismissible overlay when opening `#/giza`.
